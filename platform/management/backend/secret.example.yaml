# Example only. Do not apply this file as-is.
# Do not commit real secret values.
# A future Launcher mode must create or verify the real Secret at runtime
# without printing or writing secret values to logs or launcher-status.json.
apiVersion: v1
kind: Secret
metadata:
  name: devdeploy-backend-secret
  namespace: devdeploy
type: Opaque
stringData:
  DATABASE_URL: "postgresql://devdeploy:<POSTGRES_PASSWORD>@devdeploy-postgres-postgresql.devdeploy.svc.cluster.local:5432/devdeploy"
  JWT_SECRET_KEY: "<CHANGE_ME_AT_LEAST_32_CHARACTERS>"
  GITHUB_WORKFLOW_TOKEN: ""
