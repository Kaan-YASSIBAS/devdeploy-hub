apiVersion: v1
kind: Service
metadata:
  name: smoke-nginx-2
  namespace: devdeploy-apps
  labels:
    app.kubernetes.io/name: smoke-nginx-2
    app.kubernetes.io/managed-by: devdeploy
    app.kubernetes.io/part-of: devdeploy-workloads
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: smoke-nginx-2
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
