apiVersion: v1
kind: Service
metadata:
  name: try-nginx
  namespace: devdeploy-apps
  labels:
    app.kubernetes.io/name: try-nginx
    app.kubernetes.io/managed-by: devdeploy
    app.kubernetes.io/part-of: devdeploy-workloads
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: try-nginx
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
